http:
  routers:
    backend:
      rule: "PathPrefix(`/`)"
      service: backend
      entryPoints:
        - web

  services:
    backend:
      loadBalancer:
        servers:
          - url: "http://backend:8080"
