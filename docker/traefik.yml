http:
  routers:
    api:
      rule: "PathPrefix(`/api`)"
      service: nexus
      entryPoints:
        - web
    web:
      rule: "PathPrefix(`/`)"
      service: web
      entryPoints:
        - web
      priority: 1

  services:
    nexus:
      loadBalancer:
        servers:
          - url: "http://nexus:8080"
    web:
      loadBalancer:
        servers:
          - url: "http://web:5173"
